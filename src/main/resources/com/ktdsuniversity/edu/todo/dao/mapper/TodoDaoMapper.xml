<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktdsuniversity.edu.todo.dao.TodoDao">

	<select id="selectAllTodo" resultType="com.ktdsuniversity.edu.todo.vo.TodoVO">
	SELECT ID
	 	 , SUBJECT
	 	 , STATUS
	 	 , TO_CHAR(DEADLINE, 'YYYY-MM-DD') DEADLINE
  	  FROM TODO
  	 ORDER BY ID DESC
	</select>
	
	<select id="selectOneTodo" parameterType="_int" resultType="com.ktdsuniversity.edu.todo.vo.TodoVO">
	SELECT ID
	 	 , SUBJECT
	 	 , STATUS
	 	 , TO_CHAR(DEADLINE, 'YYYY-MM-DD') DEADLINE
  	  FROM TODO
 	 WHERE ID = #{_parameter}
	</select>
	
	<insert id="insertNewTodo" parameterType="com.ktdsuniversity.edu.todo.vo.WriteTodoVO">
	INSERT INTO TODO
 	 (ID
	, SUBJECT
	, STATUS
	, DEADLINE)
	VALUES
 	 (SEQ_TODO_PK.NEXTVAL
	, #{subject}
	, NULL
	, #{deadline})
	</insert>
	
	<update id="updateOneTodoStatus" parameterType = "com.ktdsuniversity.edu.todo.vo.TodoVO">
	UPDATE TODO
   	   SET STATUS = 'DONE'
 	 WHERE ID = #{id}
	</update>
	
	<delete id="deleteOneTodo" parameterType="_int">
	DELETE
  	  FROM TODO
 	 WHERE ID = #{_parameter}
	</delete>
</mapper>