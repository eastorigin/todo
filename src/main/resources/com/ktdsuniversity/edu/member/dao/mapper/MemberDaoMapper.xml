<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper>
	<insert id="insertNewMember" parameterType="com.ktdsuniversity.edu.member.vo.RegistMemberVO">
		INSERT INTO MEMBERS
   		   EMAIL
 		 , ID
 		 , PASSWORD
 		 , SALT
		VALUES (
   		   #{email}
 		 , #{id}
 		 , #{password}
 		 , #{salt} )
	</insert>

	<select id="selectEmail" parameterType="string" resultType="_int">
		SELECT COUNT(1)
  		  FROM MEMBERS
 		 WHERE EMAIL = #{_parameter}
	</select>
	
	<select id="selectId" parameterType="string" resultType="_int">
		SELECT COUNT(1)
  		  FROM MEMBERS
 		 WHERE ID = #{_parameter}
	</select>
	
	<select id="selectSalt" parameterType="string" resultType="_int">
		SELECT SALT
  		  FROM MEMBERS
 		 WHERE EMAIL = #{_parameter}
	</select>
</mapper>